-r requirements.txt
pytest==8.2.0
# NOTE: httpx>=0.28 removed the TestClient(app=...) shortcut that
# starlette==0.36 (pinned via fastapi==0.110.0) relies on. Keep httpx pinned
# below 0.28 until fastapi/starlette are upgraded together.
httpx==0.27.0
