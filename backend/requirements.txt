fastapi==0.110.0
uvicorn==0.27.1
sqlalchemy==2.0.28
pydantic==2.6.4
pydantic-settings==2.2.1
alembic==1.13.1
psycopg[binary]
geoalchemy2==0.14.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4's bcrypt backend-detection code is incompatible with
# bcrypt>=4.1 (fails with "password cannot be longer than 72 bytes" during
# its own internal version probe, not from any real password). Pin bcrypt
# explicitly until passlib is upgraded.
bcrypt==4.0.1
python-multipart==0.0.9
websockets==12.0
redis==5.0.3
firebase-admin==6.5.0
boto3==1.34.69
shapely>=2.0.3
